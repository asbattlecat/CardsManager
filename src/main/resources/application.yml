spring:
  liquibase:
    enabled: true
    change-log: classpath:db/migration/db_migration.yaml
    default-schema: public
    drop-first: false

  datasource:
    url: jdbc:postgresql://localhost:5432/bankcards_db
    username: postgres
    password: Bhkfylbz
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
